global:
  scrape_interval: 5s
  evaluation_interval: 5s
  external_labels:
    environment: resilience-staging

rule_files:
  - /etc/prometheus/alerts.yaml

alerting:
  alertmanagers:
    - static_configs:
        - targets: [alertmanager:9093]

scrape_configs:
  - job_name: exchange-backend
    metrics_path: /internal/metrics
    static_configs:
      - targets: [backend-a:5000, backend-b:5000]
  - job_name: otel-collector
    static_configs:
      - targets: [otel-collector:8888]
