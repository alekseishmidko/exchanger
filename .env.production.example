# Безопасный production template: credentials и connection strings запрещены.
NODE_ENV=production
# Compose имеет проверенные digest defaults; deployment может передать новые
# POSTGRES_IMAGE/CADDY_IMAGE только в формате repository:tag@sha256:<digest>.
RUNTIME_PROFILE=production
COMMAND_STORE_ADAPTER=postgres
LEDGER_STORE_ADAPTER=postgres
EVENT_LOG_ADAPTER=postgres-outbox
IDEMPOTENCY_STORE_ADAPTER=postgres
AUDIT_STORE_ADAPTER=postgres
SEQUENCER_STORE_ADAPTER=postgres
PROJECTION_STORE_ADAPTER=postgres
ADMISSION_CONTROL_ADAPTER=postgres
INSTANCE_ID=exchange-backend-1
PARTITION_LEASE_TTL_MS=15000
DEPENDENCY_PROBE_TIMEOUT_MS=1000
# POSTGRES_PASSWORD и прочие credentials передаются deployment secret store.
POSTGRES_POOL_MAX=20
POSTGRES_CONNECTION_TIMEOUT_MS=2000
POSTGRES_QUERY_TIMEOUT_MS=2000
POSTGRES_IDLE_TIMEOUT_MS=30000
WORKERS_ENABLED=true
WORKER_BATCH_SIZE=100
WORKER_CONCURRENCY=1
WORKER_TIMEOUT_MS=5000
WORKER_MAX_ATTEMPTS=3
WORKER_BASE_BACKOFF_MS=25
WORKER_MAX_BACKOFF_MS=1000
WORKER_POLL_INTERVAL_MS=1000
SERVICE_NAME=exchange-backend
BUILD_VERSION=replace-at-deploy
HOST=0.0.0.0
PORT=5000
APPLICATION_PUBLIC_URL=https://exchange.example.com
SWAGGER_ENABLED=false
SWAGGER_PATH=docs
WEBSOCKET_ALLOWED_ORIGINS=https://exchange.example.com
HTTP_ALLOWED_ORIGINS=https://exchange.example.com
TRUSTED_PROXY_CIDRS=172.16.0.0/12
WEBSOCKET_MAX_SUBSCRIBERS=10000
WEBSOCKET_MAX_PENDING=100
WEBSOCKET_MAX_CONNECTIONS_PER_IP=20
WEBSOCKET_MAX_MESSAGES_PER_WINDOW=120
WEBSOCKET_MESSAGE_WINDOW_MS=60000
WEBSOCKET_MAX_SUBSCRIPTIONS_PER_SOCKET=20
LOG_SAMPLE_LIMIT=100
LOG_SAMPLE_WINDOW_MS=10000
GATEWAY_RATE_LIMIT=60
GATEWAY_RATE_WINDOW_MS=60000
AUTH_RATE_GLOBAL_LIMIT=1000
# Обязательные identity secrets передаются secret store: AUTH_PASSWORD_PEPPER,
# AUTH_TOKEN_HASH_SECRET, AUTH_DUMMY_PASSWORD_HASH и AUTH_REDIS_URL (rediss+ACL).
# AUTH_RECOVERY_DELIVERY_URL/SECRET также обязательны и передаются deployment-средой.
AUTH_RECOVERY_DECOY_EMAIL=security-decoy@example.com
AUTH_USER_STORE_ADAPTER=postgres
AUTH_SESSION_STORE_ADAPTER=redis
AUTH_API_KEY_STORE_ADAPTER=postgres
AUTH_TOKEN_TRANSPORT=cookie
AUTH_COOKIE_NAME=__Host-exchange_session
AUTH_CSRF_COOKIE_NAME=__Host-exchange_csrf
AUTH_COOKIE_SECURE=true
AUTH_COOKIE_SAME_SITE=Strict
AUTH_SESSION_IDLE_TTL_SECONDS=1800
AUTH_SESSION_ABSOLUTE_TTL_SECONDS=604800
AUTH_MAX_SESSIONS_PER_USER=10
AUTH_RECOVERY_TTL_SECONDS=900
AUTH_RECOVERY_MIN_RESPONSE_MS=250
AUTH_RECOVERY_DELIVERY_TIMEOUT_MS=2000
AUTH_RECOVERY_DELIVERY_MAX_ATTEMPTS=3
AUTH_RECOVERY_DELIVERY_BACKOFF_MS=100
AUTH_RETENTION_INTERVAL_MS=3600000
AUTH_RETENTION_BATCH_SIZE=500
AUTH_REDIS_COMMAND_TIMEOUT_MS=750
AUTH_SCRYPT_COST=16384
AUTH_PASSWORD_PEPPER_VERSION=v1
WEBSOCKET_AUTH_RECHECK_MS=5000
AUTH_TEST_BYPASS_ENABLED=false
OTEL_TRACES_ENABLED=false
OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://otel-collector:4318/v1/traces
OTEL_BSP_MAX_QUEUE_SIZE=2048
OTEL_BSP_MAX_EXPORT_BATCH_SIZE=256
OTEL_BSP_SCHEDULE_DELAY=5000
OTEL_BSP_EXPORT_TIMEOUT=1000
OTEL_DIAGNOSTIC_SPAN_LIMIT=1000
