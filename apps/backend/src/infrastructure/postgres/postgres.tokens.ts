/** DI-токен общего пула PostgreSQL. */
export const POSTGRES_POOL = Symbol('POSTGRES_POOL');

/** DI-токен менеджера транзакционного контекста PostgreSQL. */
export const POSTGRES_TRANSACTION = Symbol('POSTGRES_TRANSACTION');
